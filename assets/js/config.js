// Purpose: Public Supabase details for the browser (PostgREST + RLS).
// Get these in Supabase → Settings → API (Project URL + anon key).
export const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
export const SUPABASE_ANON_KEY = "YOUR-PUBLIC-ANON-JWT";
